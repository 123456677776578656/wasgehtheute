(()=>{const DATA=window.EVENTS||[],root=document.getElementById('landingEvents'),mode=document.body.dataset.mode||'',today=new Intl.DateTimeFormat('en-CA',{timeZone:'Europe/Zurich'}).format(new Date());function slugify(s){return s.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/ä/g,'ae').replace(/ö/g,'oe').replace(/ü/g,'ue').replace(/ß/g,'ss').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')}function url(e){return `event.html?id=${encodeURIComponent(slugify(e.title+'-'+e.city+'-'+e.start))}`}function weekend(){const d=new Date(today+'T12:00:00'),dow=d.getDay();let add=(5-dow+7)%7;if(dow===6)add=-1;if(dow===0)add=-2;const f=new Date(d);f.setDate(d.getDate()+add);const s=new Date(f);s.setDate(f.getDate()+2);const y=x=>`${x.getFullYear()}-${String(x.getMonth()+1).padStart(2,'0')}-${String(x.getDate()).padStart(2,'0')}`;return[y(f),y(s)]}let arr=DATA.filter(e=>e.end>=today);if(mode==='buchs')arr=arr.filter(e=>e.city.includes('Buchs')||e.city.includes('Werdenberg'));if(mode==='zuerich')arr=arr.filter(e=>e.region==='Kanton Zürich');if(mode==='chur')arr=arr.filter(e=>e.region==='Chur / Graubünden');if(mode==='weekend'){const w=weekend();arr=arr.filter(e=>e.start<=w[1]&&e.end>=w[0])}arr.sort((a,b)=>a.start.localeCompare(b.start));root.innerHTML=arr.slice(0,40).map(e=>`<article class="landing-event"><small>${e.date} · 📍 ${e.city}</small><h3><a href="${url(e)}">${e.emoji} ${e.title}</a></h3><p>${e.desc}</p><a class="source" href="${url(e)}">Details →</a></article>`).join('')||'<p>Aktuell sind keine passenden kommenden Events eingetragen.</p>';document.getElementById('landingCount').textContent=arr.length;})();