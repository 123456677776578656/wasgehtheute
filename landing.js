(()=>{const DATA=window.EVENTS||[],root=document.getElementById('landingEvents'),mode=document.body.dataset.mode||'',cityFilter=document.body.dataset.city||'',regionFilter=document.body.dataset.region||'',today=new Intl.DateTimeFormat('en-CA',{timeZone:'Europe/Zurich'}).format(new Date());function slugify(s){return s.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/ä/g,'ae').replace(/ö/g,'oe').replace(/ü/g,'ue').replace(/ß/g,'ss').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')}function url(e){return `event.html?id=${encodeURIComponent(slugify(e.title+'-'+e.city+'-'+e.start))}`}function ymd(d){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}function weekend(){const d=new Date(today+'T12:00:00'),dow=d.getDay();let add=(5-dow+7)%7;if(dow===6)add=-1;if(dow===0)add=-2;const f=new Date(d);f.setDate(d.getDate()+add);const s=new Date(f);s.setDate(f.getDate()+2);return[ymd(f),ymd(s)]}function label(e){if(e.start<=today&&e.end>=today)return'🔥 Heute';const d=new Date(today+'T12:00:00');d.setDate(d.getDate()+1);if(e.start===ymd(d))return'🌤️ Morgen';const w=weekend();if(e.start<=w[1]&&e.end>=w[0])return'🗓️ Dieses Wochenende';return e.date}let arr=DATA.filter(e=>e.end>=today&&e.quality_status!=='cancelled-warning');if(mode==='buchs')arr=arr.filter(e=>e.city.includes('Buchs')||e.city.includes('Werdenberg'));if(mode==='zuerich')arr=arr.filter(e=>e.region==='Kanton Zürich');if(mode==='chur')arr=arr.filter(e=>e.region==='Chur / Graubünden');if(mode==='thurgau')arr=arr.filter(e=>e.region==='Kanton Thurgau');if(mode==='nightlife')arr=arr.filter(e=>(e.cats||[]).some(c=>['Bar','Club','Party'].includes(c)));if(mode==='weekend'){const w=weekend();arr=arr.filter(e=>e.start<=w[1]&&e.end>=w[0])}if(cityFilter)arr=arr.filter(e=>String(e.city).toLowerCase().includes(cityFilter.toLowerCase()));if(regionFilter)arr=arr.filter(e=>e.region===regionFilter);arr.sort((a,b)=>a.start.localeCompare(b.start));root.innerHTML=arr.slice(0,60).map(e=>`<article class="landing-event"><small><strong>${label(e)}</strong> · 📍 ${e.city}${e.venue?` · ${e.venue}`:''}</small><h3><a href="${url(e)}">${e.emoji} ${e.title}</a></h3><p>${e.desc}</p><div class="event-facts">${(e.cats||[]).includes('Gratis')?'<span>🆓 Gratis</span>':''}${e.price?`<span>💳 ${e.price}</span>`:''}${e.ticket?'<span>🎟️ Tickets</span>':''}</div><a class="source" href="${url(e)}">Details →</a></article>`).join('')||'<p>Aktuell sind keine passenden kommenden Events eingetragen.</p>';document.getElementById('landingCount').textContent=arr.length;})();